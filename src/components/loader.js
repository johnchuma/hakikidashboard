const Loader = () => {
  return (
    <div className="flex justify-center py-24">
      <div className="border-4 size-8 border-b-transparent border-primaryColor animate-spin rounded-full"></div>
    </div>
  );
};

export default Loader;
